[
  {
    "eventId": "api-ingress-traffic",
    "category": "traffic",
    "glyph": "api-ingress",
    "eventName": "Control-Plane / API Ingress",
    "description": "Tracing an 'oc'/'kubectl' request to the hosted cluster's API. It enters the management cluster through the Shared Ingress proxy's MetalLB VIP, is SNI-routed by hostname to this guest's kube-apiserver Service, and terminates at the Guest API Server Pod in the HCP namespace. This control-plane path stays entirely in the management cluster and never enters a guest worker VM. The OAuth, Konnectivity, and Ignition endpoints ride the same shared ingress.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "svc-ingress-lb-shared",
        "description": "An 'oc' client resolves the guest's API hostname (api.<guest>) to the Shared Ingress LoadBalancer VIP. With no cloud LB present, a MetalLB speaker answers ARP for the VIP, so the request lands on the bare metal cluster."
      },
      {
        "step": 2,
        "sourceComponentId": "svc-ingress-lb-shared",
        "targetComponentId": "shared-ingress-proxy",
        "description": "The LoadBalancer Service DNATs the VIP to a healthy Shared Ingress Proxy endpoint. HAProxy reads the TLS SNI hostname and selects this hosted cluster's control-plane backend (the same proxy fronts OAuth, Konnectivity, and Ignition by hostname too)."
      },
      {
        "step": 3,
        "sourceComponentId": "shared-ingress-proxy",
        "targetComponentId": "guest-api-server",
        "description": "The proxy forwards the connection to this guest's kube-apiserver Service in the HCP namespace. The Guest API Server Pod terminates the request, authenticates it, and serves it from etcd — the traffic never leaves the management control plane."
      }
    ]
  },
  {
    "eventId": "app-ingress-traffic",
    "category": "traffic",
    "glyph": "app-ingress",
    "eventName": "Application Ingress (Wildcard Route)",
    "description": "Tracing an inbound HTTPS request to a guest application on the *.apps wildcard domain. Unlike API traffic, this never touches the Shared Ingress Proxy: it enters through the Apps Ingress LoadBalancer (an infra-side LoadBalancer Service on the bare metal cluster whose VIP MetalLB advertises, which forwards to the guest router's NodePort), crosses into the KubeVirt VM, and is handled by the guest cluster's own OpenShift Ingress Router before reaching the front-end ClusterIP Service and Pod. There is no LoadBalancer inside the guest — its ingress is a NodePort fronted from the management cluster.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "svc-apps-lb-infra",
        "description": "The client resolves an app hostname under *.apps.<guest-domain> to the Apps Ingress LoadBalancer VIP. This infra-side LoadBalancer lives in the guest's HCP namespace on the management cluster; a MetalLB speaker answers ARP for its VIP, so the packet lands on the bare metal cluster. The control-plane Shared Ingress Proxy is not involved."
      },
      {
        "step": 2,
        "sourceComponentId": "svc-apps-lb-infra",
        "targetComponentId": "ovs-host",
        "description": "The management-cluster LoadBalancer Service translates the VIP toward the guest router's NodePort on a selected worker VM IP. The packet enters that VM's bare metal host through OVS br-int, whose OVN flows steer it toward the KubeVirt Launcher and tap interface for the selected guest worker."
      },
      {
        "step": 3,
        "sourceComponentId": "ovs-host",
        "targetComponentId": "kubevirt-launcher",
        "description": "OVS routes the packet through the tap0 virtual interface into the KubeVirt Launcher Pod, crossing the host-to-VM network boundary."
      },
      {
        "step": 4,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The KubeVirt Launcher delivers the packet to the virtio-net NIC of the Guest Worker Node VM, shifting context from the host kernel to the guest RHCOS kernel."
      },
      {
        "step": 5,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "ovs-guest",
        "description": "The guest RHCOS kernel hands the packet to the in-VM Open vSwitch service, which evaluates guest OVN flow rules for the router-default Service."
      },
      {
        "step": 6,
        "sourceComponentId": "ovs-guest",
        "targetComponentId": "svc-router-nodeport-default",
        "description": "The packet arrives on the worker VM at the router's NodePort (router-nodeport-default) — the guest's external ingress handle. Guest OVS matches the NodePort Service and applies its OVN load-balancer flow. There is no guest LoadBalancer here; the LoadBalancer that fronted this NodePort lives back on the management cluster."
      },
      {
        "step": 7,
        "sourceComponentId": "svc-router-nodeport-default",
        "targetComponentId": "openshift-ingress-router-guest",
        "description": "The NodePort Service DNATs to an OpenShift Ingress Router Pod inside the VM, which terminates TLS and matches the request against its Route table for the *.apps hostname."
      },
      {
        "step": 8,
        "sourceComponentId": "openshift-ingress-router-guest",
        "targetComponentId": "svc-frontend",
        "description": "The guest Ingress Router forwards the request to the Front-End Service — the ClusterIP that fronts the frontend Pods — addressing its stable virtual IP rather than a Pod directly."
      },
      {
        "step": 9,
        "sourceComponentId": "svc-frontend",
        "targetComponentId": "frontend-application-pod",
        "description": "OVN load-balancer flows DNAT the ClusterIP to a ready Front-End Application Pod endpoint. The application process accepts the connection and returns a response."
      }
    ]
  },
  {
    "eventId": "pod-spawning",
    "category": "lifecycle",
    "glyph": "spawn",
    "eventName": "Guest Pod Lifecycle (Spawn)",
    "description": "Tracing how a new application Deployment becomes a running Pod in the guest cluster: the Guest API Server persists intent, the Deployment and ReplicaSet loops create the Pod, the scheduler binds it, and the guest kubelet asks CRI-O to start it inside the worker VM.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "A developer runs 'oc apply -f deployment.yaml'. The Guest API Server validates the request and persists the Deployment desired state in Guest Etcd. No Pod exists yet."
      },
      {
        "step": 2,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-controller-manager",
        "description": "The Deployment controller inside the Guest Controller Manager observes the new Deployment, creates a ReplicaSet, and the ReplicaSet controller creates the requested Pod object through the API Server. These are control loops inside one controller-manager process, not separate Pods."
      },
      {
        "step": 3,
        "sourceComponentId": "guest-controller-manager",
        "targetComponentId": "guest-api-server",
        "description": "The Guest Controller Manager writes the ReplicaSet and unscheduled Pod records to the Guest API Server. The Pod now exists as desired state, but it has no nodeName and no container is running."
      },
      {
        "step": 4,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-kube-scheduler",
        "description": "The Guest Scheduler watches for Pods with no node assignment. It evaluates guest worker capacity, taints, affinity, and topology constraints, then selects a Guest Worker Node VM."
      },
      {
        "step": 5,
        "sourceComponentId": "guest-kube-scheduler",
        "targetComponentId": "guest-api-server",
        "description": "The Scheduler writes the binding through the Guest API Server, setting the Pod's nodeName to the selected guest worker."
      },
      {
        "step": 6,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubelet-guest",
        "description": "The guest kubelet observes the newly bound Pod through its own outbound watch of the Guest API Server. The API Server does not push the PodSpec through Konnectivity; that tunnel is reserved for API-Server-initiated traffic such as exec, logs, and port-forward."
      },
      {
        "step": 7,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "crio-guest",
        "description": "Kubelet issues RunPodSandbox and CreateContainer CRI calls. Guest CRI-O pulls the image, creates the cgroup and namespaces, and invokes the CNI path to attach the Pod to guest OVN-Kubernetes."
      },
      {
        "step": 8,
        "sourceComponentId": "crio-guest",
        "targetComponentId": "frontend-application-pod",
        "description": "Guest CRI-O starts the container via runc. The application process becomes PID 1 inside its namespaces, registers with the guest network, and the Pod transitions to Running status."
      }
    ]
  },
  {
    "eventId": "east-west-service-traffic",
    "category": "traffic",
    "glyph": "east-west",
    "eventName": "East-West Service Traffic (Front-End → Back-End)",
    "description": "Tracing an in-cluster call from the Front-End Application to the Back-End Application through the Back-End ClusterIP Service: name resolution to a virtual IP, OVN load-balancer DNAT on the guest OVS fabric, a NetworkPolicy ACL check, and delivery to a backing Pod — no ingress, no traffic leaving the VM.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "frontend-application-pod",
        "targetComponentId": "svc-backend",
        "description": "The Front-End resolves backend.e-commerce-prod.svc.cluster.local to the Back-End Service's ClusterIP and opens a connection to that stable virtual IP rather than to a specific backend Pod."
      },
      {
        "step": 2,
        "sourceComponentId": "svc-backend",
        "targetComponentId": "ovs-guest",
        "description": "The connection to the ClusterIP enters the in-VM OVS br-int bridge, where the OVN-Kubernetes load-balancer flow installed for this Service matches the virtual IP and selects a healthy backend endpoint."
      },
      {
        "step": 3,
        "sourceComponentId": "ovs-guest",
        "targetComponentId": "netpol-ecommerce",
        "description": "Before the packet is delivered, the OVN logical pipeline on br-int evaluates the NetworkPolicy ACLs compiled for the backend port group. The policy admits the Front-End Pods' address set as an allowed ingress source, so the connection passes the ACL gate (traffic from any other source would be dropped here)."
      },
      {
        "step": 4,
        "sourceComponentId": "netpol-ecommerce",
        "targetComponentId": "backend-application-pod",
        "description": "With the policy allowing the flow, OVS DNATs the destination from the ClusterIP to the chosen Back-End Application Pod IP and delivers the packet over its veth pair. The backend process accepts the connection and returns its response."
      }
    ]
  },
  {
    "eventId": "hcp-provisioning",
    "category": "lifecycle",
    "glyph": "provision",
    "eventName": "HCP Control Plane + NodePool Provisioning",
    "description": "Tracing both halves of HCP creation: the cluster-wide HyperShift Operator turns HostedCluster intent into a per-HCP control plane, while its NodePool controller drives Cluster API and CAPK to create a KubeVirt worker VM that boots with Ignition and joins the guest API.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "A platform admin applies HostedCluster and NodePool manifests. These are management-cluster CRs, so the request goes to the Management Kube API Server — not to the guest API, which does not exist yet."
      },
      {
        "step": 2,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "hostedcluster-cr",
        "description": "The Management API Server persists the HostedCluster desired-state record in Management Etcd, alongside the NodePool record from the same apply (the NodePool branch picks that up in parallel at step 7). These records declare the hosted control plane; they are not themselves running components."
      },
      {
        "step": 3,
        "sourceComponentId": "hostedcluster-cr",
        "targetComponentId": "hypershift-operator",
        "description": "The cluster-wide HyperShift Operator watches HostedCluster records, creates the dedicated HCP namespace, and deploys one Control Plane Operator for this hosted cluster."
      },
      {
        "step": 4,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "control-plane-operator",
        "description": "The Control Plane Operator becomes the per-HCP owner. It reconciles the guest control-plane Deployments and StatefulSets inside the dedicated namespace."
      },
      {
        "step": 5,
        "sourceComponentId": "control-plane-operator",
        "targetComponentId": "guest-etcd",
        "description": "The Control Plane Operator deploys Guest Etcd as a StatefulSet Pod. This is not a static Pod: it runs as a normal management-cluster workload inside the HCP namespace."
      },
      {
        "step": 6,
        "sourceComponentId": "control-plane-operator",
        "targetComponentId": "guest-api-server",
        "description": "Once etcd is available, the Control Plane Operator brings up the Guest API Server and the other hosted endpoints, including OAuth, Konnectivity, and Ignition, as management-cluster Deployments."
      },
      {
        "step": 7,
        "sourceComponentId": "nodepool-cr",
        "targetComponentId": "hypershift-operator",
        "description": "In parallel, the HyperShift Operator's NodePool controller observes the NodePool record and renders it into Cluster API Cluster and MachineDeployment desired state."
      },
      {
        "step": 8,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "capi-manager",
        "description": "Cluster API Manager expands the MachineDeployment's desired replicas into MachineSet and Machine objects for the guest workers."
      },
      {
        "step": 9,
        "sourceComponentId": "capi-manager",
        "targetComponentId": "capk-provider",
        "description": "Cluster API delegates each Machine's infrastructure to CAPK through a KubevirtMachine record. CAPK creates the corresponding KubeVirt VirtualMachine object in the management cluster."
      },
      {
        "step": 10,
        "sourceComponentId": "capk-provider",
        "targetComponentId": "kubevirt-vm-cr",
        "description": "CAPK creates the KubeVirt VirtualMachine desired-state record in the management cluster; it does not create a launcher Pod directly."
      },
      {
        "step": 11,
        "sourceComponentId": "kubevirt-vm-cr",
        "targetComponentId": "kubevirt-launcher",
        "description": "KubeVirt's virt-controller observes the VirtualMachine/VMI and creates its virt-launcher Pod. The management scheduler places that Pod on a bare metal worker."
      },
      {
        "step": 12,
        "sourceComponentId": "virt-handler",
        "targetComponentId": "kubevirt-launcher",
        "description": "virt-handler on the selected bare metal node signals the launcher's libvirt process to start the qemu-kvm domain. virt-handler drives node-local VM boot; it does not create the launcher Pod."
      },
      {
        "step": 13,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The launcher starts the RHCOS VirtualMachineInstance, creating the guest CPU, memory, disks, and virtio network device inside the qemu-kvm process."
      },
      {
        "step": 14,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "ignition-server",
        "description": "During first boot, RHCOS requests its Ignition payload over TLS. The response provides the bootstrap credentials and machine configuration needed to join this hosted cluster."
      },
      {
        "step": 15,
        "sourceComponentId": "konnectivity-agent",
        "targetComponentId": "konnectivity-server",
        "description": "The Konnectivity Agent inside the VM establishes its persistent outbound tunnel to the hosted Konnectivity Server. The tunnel is now available for later API-Server-initiated exec, logs, port-forward, webhook, and metrics traffic."
      },
      {
        "step": 16,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "guest-api-server",
        "description": "Separately, the guest kubelet connects outbound to the Guest API Server, registers its Node, and posts status until it becomes Ready. Pod watches use this direct outbound API connection, not Konnectivity."
      }
    ]
  },
  {
    "eventId": "nodepool-scale-up",
    "category": "lifecycle",
    "glyph": "scale-up",
    "eventName": "NodePool Scale-Up (add a worker)",
    "description": "Tracing how raising a NodePool's replica count becomes a brand-new RHCOS worker VM: the HyperShift NodePool controller renders it into Cluster API, CAPK turns the desired Machine into a KubeVirt VirtualMachine, and virt-handler boots the VMI until it registers as a guest node.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "A platform admin runs 'oc scale nodepool <name> --replicas=N+1'. The NodePool lives in the management cluster, so the Management Kube API Server persists the higher desired replica count."
      },
      {
        "step": 2,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "hypershift-operator",
        "description": "The HyperShift Operator's NodePool controller observes that desired replicas now exceed the running workers and renders the NodePool into updated Cluster API objects (MachineDeployment / MachineSet)."
      },
      {
        "step": 3,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "capi-manager",
        "description": "The Cluster API Manager reconciles the scaled MachineSet by creating one new Machine object to represent the additional worker."
      },
      {
        "step": 4,
        "sourceComponentId": "capi-manager",
        "targetComponentId": "capk-provider",
        "description": "Cluster API delegates infrastructure creation for the new Machine to the cluster-api-provider-kubevirt (CAPK)."
      },
      {
        "step": 5,
        "sourceComponentId": "capk-provider",
        "targetComponentId": "kubevirt-vm-cr",
        "description": "CAPK creates a KubeVirt VirtualMachine record for the new Machine. That record is the persistent desired state for the worker VM."
      },
      {
        "step": 6,
        "sourceComponentId": "kubevirt-vm-cr",
        "targetComponentId": "kubevirt-launcher",
        "description": "KubeVirt's virt-controller creates the VMI and its virt-launcher Pod; the management scheduler places the launcher on a bare metal worker."
      },
      {
        "step": 7,
        "sourceComponentId": "virt-handler",
        "targetComponentId": "kubevirt-launcher",
        "description": "virt-handler on that bare metal node signals the launcher Pod's libvirt to boot the qemu-kvm domain (it drives the boot on the node; it does not create the Pod)."
      },
      {
        "step": 8,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The launcher starts the RHCOS VirtualMachineInstance for the new worker."
      },
      {
        "step": 9,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "ignition-server",
        "description": "As it boots, the VM fetches its Ignition config over TLS, receiving the kubelet bootstrap credentials and machine config needed to join the guest cluster."
      },
      {
        "step": 10,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "guest-api-server",
        "description": "The new node's kubelet registers with the Guest API Server and goes Ready; the NodePool's desired replica count is now satisfied."
      }
    ]
  },
  {
    "eventId": "nodepool-scale-down",
    "category": "lifecycle",
    "glyph": "scale-down",
    "eventName": "NodePool Scale-Down (remove a worker)",
    "description": "Tracing the ordered removal of a guest worker: lowering NodePool replicas makes Cluster API select a Machine, cordon and drain its guest Node, wait for volumes to detach, then ask CAPK to delete the KubeVirt infrastructure before removing the Node record.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "The admin scales the NodePool down. The Management Kube API Server records the lower desired replica count."
      },
      {
        "step": 2,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "hypershift-operator",
        "description": "The HyperShift Operator's NodePool controller sees more workers running than desired and scales the MachineDeployment down."
      },
      {
        "step": 3,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "capi-manager",
        "description": "The Cluster API Manager selects a Machine to remove and marks it for deletion."
      },
      {
        "step": 4,
        "sourceComponentId": "capi-manager",
        "targetComponentId": "guest-api-server",
        "description": "Before deleting infrastructure, the Cluster API Machine controller uses the guest API to cordon the selected Node and evict drainable workloads. It waits for the drain and attached-volume cleanup to finish; shutdown does not happen first."
      },
      {
        "step": 5,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "capi-manager",
        "description": "When the guest Node is drained and its VolumeAttachments are gone, the workload-cluster side of Machine deletion is complete and Cluster API can continue."
      },
      {
        "step": 6,
        "sourceComponentId": "capi-manager",
        "targetComponentId": "capk-provider",
        "description": "Cluster API now deletes the KubevirtMachine infrastructure object. CAPK removes the backing KubeVirt VirtualMachine from the management cluster."
      },
      {
        "step": 7,
        "sourceComponentId": "capk-provider",
        "targetComponentId": "kubevirt-vm-cr",
        "description": "CAPK removes the KubeVirt VirtualMachine desired-state record after the infrastructure deletion request reaches the provider."
      },
      {
        "step": 8,
        "sourceComponentId": "kubevirt-vm-cr",
        "targetComponentId": "kubevirt-launcher",
        "description": "KubeVirt responds to VirtualMachine deletion by terminating the VirtualMachineInstance and its virt-launcher Pod."
      },
      {
        "step": 9,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The launcher shuts the RHCOS VMI down; qemu-kvm exits and the VM's tap interface is removed from the host network."
      },
      {
        "step": 10,
        "sourceComponentId": "capi-manager",
        "targetComponentId": "guest-api-server",
        "description": "After the infrastructure is gone, Cluster API deletes the stale guest Node record. The NodePool, MachineDeployment, MachineSet, and Ready worker count now agree."
      }
    ]
  },
  {
    "eventId": "oc-exec-konnectivity",
    "category": "traffic",
    "glyph": "exec",
    "eventName": "oc exec into a Pod (Konnectivity)",
    "description": "Tracing an 'oc exec' from a developer to a process inside a guest application Pod. Because the Guest API Server cannot route to the node network directly, the call rides the API-Server-initiated Konnectivity tunnel down to the node's kubelet and CRI-O.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "A developer runs 'oc exec -it <pod> -- sh'. The request authenticates against the Guest API Server, which must reach the kubelet on the node hosting the Pod."
      },
      {
        "step": 2,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "konnectivity-server",
        "description": "The Guest API Server cannot dial the guest node network directly, so it hands the exec stream to the Konnectivity Server — the one direction the tunnel carries (API-Server-initiated traffic)."
      },
      {
        "step": 3,
        "sourceComponentId": "konnectivity-server",
        "targetComponentId": "konnectivity-agent",
        "description": "The server proxies the call through the persistent tunnel to the Konnectivity Agent running inside the target worker VM."
      },
      {
        "step": 4,
        "sourceComponentId": "konnectivity-agent",
        "targetComponentId": "kubelet-guest",
        "description": "The agent forwards the request to the guest kubelet on that node."
      },
      {
        "step": 5,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "crio-guest",
        "description": "The kubelet issues an Exec CRI call to guest CRI-O for the target container."
      },
      {
        "step": 6,
        "sourceComponentId": "crio-guest",
        "targetComponentId": "frontend-application-pod",
        "description": "CRI-O enters the container's namespaces and runs the command; stdout/stderr stream back up the same tunnel to the developer's terminal."
      }
    ]
  },
  {
    "eventId": "in-cluster-dns",
    "category": "traffic",
    "glyph": "dns",
    "eventName": "In-Cluster DNS Resolution",
    "description": "Tracing how an application Pod turns a Service name into a ClusterIP: the Pod's stub resolver queries the cluster DNS Service, a CoreDNS Pod answers from records synchronized from the guest API, and the caller then opens the real connection.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "frontend-application-pod",
        "targetComponentId": "coredns-node",
        "description": "The Front-End looks up 'backend.e-commerce-prod.svc.cluster.local'. Its /etc/resolv.conf points at the cluster DNS Service, whose endpoint is a CoreDNS Pod running on a guest worker; CoreDNS is not assumed to be node-local to the caller."
      },
      {
        "step": 2,
        "sourceComponentId": "coredns-node",
        "targetComponentId": "guest-api-server",
        "description": "CoreDNS resolves cluster-local names from the Service and EndpointSlice records it syncs from the Guest API Server (external names would instead be forwarded to the upstream resolvers)."
      },
      {
        "step": 3,
        "sourceComponentId": "coredns-node",
        "targetComponentId": "frontend-application-pod",
        "description": "CoreDNS returns the Back-End Service's ClusterIP to the caller and caches the record for subsequent lookups."
      },
      {
        "step": 4,
        "sourceComponentId": "frontend-application-pod",
        "targetComponentId": "svc-backend",
        "description": "With the name resolved, the Front-End opens its connection to the Back-End ClusterIP — where the east-west Service flow takes over."
      }
    ]
  },
  {
    "eventId": "fail-worker-vm-crash",
    "category": "failure",
    "glyph": "vm-failure",
    "eventName": "Failure: a worker VM crashes (reconcile)",
    "description": "A guest worker VM dies unexpectedly. virt-handler reports the failed VMI, and KubeVirt reconciles the existing VirtualMachine's runStrategy by creating a fresh VMI and launcher. Cluster API does not replace the Machine for an ordinary guest crash because the worker infrastructure object still exists.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "virt-handler",
        "description": "The RHCOS VMI's qemu-kvm process dies. virt-handler on the bare metal node detects that the libvirt domain has stopped."
      },
      {
        "step": 2,
        "sourceComponentId": "virt-handler",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "virt-handler updates the VirtualMachineInstance's status to Failed on the Management Kube API Server."
      },
      {
        "step": 3,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "kubevirt-vm-cr",
        "description": "The KubeVirt VirtualMachine remains the desired-state owner. With runStrategy Always (or RerunOnFailure for a failed exit), its controller sees that no healthy VMI is running and requests a replacement instance."
      },
      {
        "step": 4,
        "sourceComponentId": "kubevirt-vm-cr",
        "targetComponentId": "kubevirt-launcher",
        "description": "KubeVirt's virt-controller creates a new VMI and its virt-launcher Pod from the existing VirtualMachine template. It is not a new CAPI Machine or a new CAPK VirtualMachine."
      },
      {
        "step": 5,
        "sourceComponentId": "mgmt-scheduler",
        "targetComponentId": "kubevirt-launcher",
        "description": "The management scheduler binds the replacement launcher Pod to a bare metal worker with sufficient capacity."
      },
      {
        "step": 6,
        "sourceComponentId": "virt-handler",
        "targetComponentId": "kubevirt-launcher",
        "description": "virt-handler on the selected host signals the new launcher's libvirt process to boot the qemu-kvm domain."
      },
      {
        "step": 7,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The replacement VMI boots the same RHCOS worker identity. Its kubelet reconnects to the Guest API Server and the existing Node returns Ready; desired state is restored without replacing the CAPI Machine."
      }
    ]
  },
  {
    "eventId": "fail-launcher-evicted",
    "category": "failure",
    "glyph": "pod-evicted",
    "eventName": "Failure: launcher Pod evicted (reconcile)",
    "description": "Resource pressure evicts a virt-launcher Pod and stops its VMI. KubeVirt — not CAPK — recreates the launcher for the existing VMI/VirtualMachine intent and the management scheduler places it on a host with capacity. (A live-migratable VMI with an appropriate eviction strategy may migrate instead; this trace models restart.)",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "kubelet-host",
        "targetComponentId": "kubevirt-launcher",
        "description": "Under memory/CPU pressure the host kubelet evicts the virt-launcher Pod; its qemu-kvm process is killed and the VM goes down."
      },
      {
        "step": 2,
        "sourceComponentId": "kubelet-host",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "The kubelet reports the launcher Pod as Evicted/Failed to the Management Kube API Server."
      },
      {
        "step": 3,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "kubevirt-vm-cr",
        "description": "KubeVirt's controllers observe that the launcher is gone and reconcile the still-running VirtualMachine/VMI intent. This is a KubeVirt workload recovery path, not a Cluster API Machine replacement."
      },
      {
        "step": 4,
        "sourceComponentId": "kubevirt-vm-cr",
        "targetComponentId": "kubevirt-launcher",
        "description": "virt-controller creates a fresh virt-launcher Pod from the existing VM/VMI specification."
      },
      {
        "step": 5,
        "sourceComponentId": "mgmt-scheduler",
        "targetComponentId": "kubevirt-launcher",
        "description": "The management scheduler binds the replacement launcher Pod to a bare metal worker that has capacity (avoiding the pressured node)."
      },
      {
        "step": 6,
        "sourceComponentId": "virt-handler",
        "targetComponentId": "kubevirt-launcher",
        "description": "virt-handler on the selected node connects the launcher to the host's KVM/libvirt stack and starts the qemu-kvm domain."
      },
      {
        "step": 7,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The VMI boots again and its guest kubelet reconnects to the Guest API Server, returning the worker Node to Ready."
      }
    ]
  },
  {
    "eventId": "fail-control-plane-pod",
    "category": "failure",
    "glyph": "control-failure",
    "eventName": "Failure: API Server container crashes (local restart)",
    "description": "The Guest API Server container exits inside an otherwise healthy Pod. The host kubelet's local restart loop asks CRI-O to restart that container in the same Pod; the Deployment and ReplicaSet controllers only create a replacement Pod if the Pod itself is deleted, evicted, or lost with its node.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubelet-host",
        "description": "The kube-apiserver process exits. CRI-O reports the terminated container to the bare metal host's kubelet, but the Pod object and its sandbox still exist."
      },
      {
        "step": 2,
        "sourceComponentId": "kubelet-host",
        "targetComponentId": "crio-host",
        "description": "Because the Pod's restartPolicy is Always, kubelet applies backoff if needed and asks host CRI-O to restart the failed container in place. No scheduler or ReplicaSet action is required."
      },
      {
        "step": 3,
        "sourceComponentId": "crio-host",
        "targetComponentId": "guest-api-server",
        "description": "CRI-O starts a new kube-apiserver container in the existing Pod sandbox. Readiness succeeds, the Service endpoint becomes available again, and the hosted API resumes serving."
      }
    ]
  },
  {
    "eventId": "configmap-update-rollout",
    "category": "state",
    "glyph": "config",
    "eventName": "ConfigMap Update → Live in a Pod",
    "description": "Tracing what really happens after 'oc apply -f configmap.yaml'. There is NO bespoke operator that rolls a plain ConfigMap edit into a running Pod: the apply only rewrites the ConfigMap record in guest etcd. Volume-mounted ConfigMaps are refreshed in place by the guest kubelet (but the app must re-read the file), while env-var and subPath consumers see nothing until the Pod is recreated. To make the new config actually live you change the desired Pod state — 'oc rollout restart' (or a config-hash annotation) bumps the Deployment's pod template, and the Deployment controller inside the Guest Controller Manager then rolls a fresh Pod that reads the updated ConfigMap at startup. (A tool like Stakater Reloader can watch ConfigMaps/Secrets and fire that rollout restart automatically — the closest thing to an operator that does this for you.)",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "A developer edits the ConfigMap and runs 'oc apply -f configmap.yaml' against the hosted cluster. The Guest API Server validates the object and increments its resourceVersion — but updating a ConfigMap does not, by itself, touch any Pod or Deployment."
      },
      {
        "step": 2,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-etcd",
        "description": "The API Server persists the new ConfigMap revision to guest etcd. This is the ONLY change a bare ConfigMap update makes: the Guest Controller Manager runs no loop that restarts workloads on a plain ConfigMap edit, so this write by itself leaves every running Pod untouched."
      },
      {
        "step": 3,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubelet-guest",
        "description": "For Pods that mount the ConfigMap as a VOLUME, the guest kubelet's configmap-and-secret manager notices the changed object on a later sync and atomically swaps the projected files. With default sync and cache settings, propagation can take roughly two minutes. The container then sees new file contents, but the application must re-read them itself (for example via a file watcher or SIGHUP). Environment-variable and subPath consumers do not receive this update and require Pod recreation."
      },
      {
        "step": 4,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "To force the new config live (the only option for env-var / subPath consumers, and the clean option for everyone else), the developer mutates the workload's DESIRED state: 'oc rollout restart deployment/<app>' — or bumping a checksum/config-hash annotation on the pod template. This patch to the Deployment is the actual desired-state change the question is about. A rollout is often preferred even when hot-reload would work: it converges every replica atomically (instead of each kubelet's independent ~1-min timer), it is gated by readiness probes so a bad config halts the rollout with old Pods still serving, and it leaves a revision you can 'rollout undo'."
      },
      {
        "step": 5,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-controller-manager",
        "description": "The Deployment controller (a reconcile loop inside the Guest Controller Manager — not a standalone operator) sees the pod-template hash changed and creates a new ReplicaSet; the ReplicaSet controller then rolls that new ReplicaSet out, bringing its replicas up while retiring the old ones per the rollout strategy."
      },
      {
        "step": 6,
        "sourceComponentId": "guest-controller-manager",
        "targetComponentId": "guest-api-server",
        "description": "The controllers write the new ReplicaSet and its Pod objects back to the API Server. The freshly desired Pods reference the updated pod template, so when they start they will pull the current ConfigMap revision from step 2."
      },
      {
        "step": 7,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-kube-scheduler",
        "description": "The new Pod has no node assignment, so the Guest Scheduler evaluates guest worker capacity and affinity and selects a Guest Worker Node VM for it."
      },
      {
        "step": 8,
        "sourceComponentId": "guest-kube-scheduler",
        "targetComponentId": "guest-api-server",
        "description": "The Scheduler writes a Binding back to the Guest API Server, assigning the replacement Pod to the chosen guest worker node."
      },
      {
        "step": 9,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubelet-guest",
        "description": "The guest kubelet observes the newly bound Pod on its own outbound watch of the Guest API Server (not via the Konnectivity tunnel, which only carries API-Server-initiated traffic such as exec/logs)."
      },
      {
        "step": 10,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "crio-guest",
        "description": "Kubelet issues RunPodSandbox / CreateContainer calls to guest CRI-O. CRI-O mounts the ConfigMap volume and injects any ConfigMap-sourced env vars from the CURRENT revision, so the new container is built around the updated config."
      },
      {
        "step": 11,
        "sourceComponentId": "crio-guest",
        "targetComponentId": "frontend-application-pod",
        "description": "CRI-O starts the container via runc; the application reads the updated ConfigMap at startup and goes Running with the new config live. Once it is Ready the old Pod is terminated and the rollout is complete."
      }
    ]
  },
  {
    "eventId": "pvc-provision-mount",
    "category": "state",
    "glyph": "storage",
    "eventName": "Storage: PVC → Provisioned PV → Mounted in a Pod",
    "description": "Tracing a guest PersistentVolumeClaim from 'oc apply' to a filesystem inside a Pod. The HCP-on-KubeVirt twist: the guest has no storage backend of its own — the KubeVirt CSI Controller provisions a backing PVC in the management cluster and hotplugs that volume into the worker VMI as a new disk, which the in-VM CSI Node Driver then formats and mounts. Day-0 setup happened earlier and stays out of the trace: the Storage Operator deployed the csi.kubevirt.io driver and its kubevirt-csi-infra-default StorageClass.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "A developer declares a PersistentVolumeClaim with 'oc apply' — 10Gi, storageClassName kubevirt-csi-infra-default. The Guest API Server authenticates the request, runs admission, and accepts the claim."
      },
      {
        "step": 2,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-etcd",
        "description": "The Guest API Server persists the PersistentVolumeClaim in Guest Etcd. The claim sits at phase Pending — pure intent, no volume exists anywhere yet."
      },
      {
        "step": 3,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubevirt-csi-controller",
        "description": "The KubeVirt CSI Controller's external-provisioner sidecar — running in the HCP namespace but watching the guest API — sees the Pending claim names its StorageClass and issues a CSI CreateVolume gRPC call to the csi.kubevirt.io driver."
      },
      {
        "step": 4,
        "sourceComponentId": "kubevirt-csi-controller",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "The driver creates a backing PersistentVolumeClaim in the management cluster's HCP namespace via the Management Kube API Server. The guest's 'cloud volume' is literally an infra PVC, bound by whatever StorageClass the management cluster runs on its bare metal."
      },
      {
        "step": 5,
        "sourceComponentId": "kubevirt-csi-controller",
        "targetComponentId": "guest-api-server",
        "description": "With the infra PVC bound, the external-provisioner creates the guest PersistentVolume: driver csi.kubevirt.io, volumeHandle pointing at the infra PVC's name. The guest now has a PV record describing storage that physically lives a cluster below it."
      },
      {
        "step": 6,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-controller-manager",
        "description": "The Guest Controller Manager's PersistentVolume controller loop binds claim to volume — it stamps each with a reference to the other and both flip to phase Bound. The claim is now usable; nothing is attached to any node yet."
      },
      {
        "step": 7,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubevirt-csi-controller",
        "description": "A Pod consuming the claim gets scheduled onto the worker node, so a VolumeAttachment object appears. The controller's external-attacher sidecar sees it and calls ControllerPublishVolume: attach this volume to that node."
      },
      {
        "step": 8,
        "sourceComponentId": "kubevirt-csi-controller",
        "targetComponentId": "kubevirt-launcher",
        "description": "Attach means hotplug here: KubeVirt surfaces the bound infra PVC into the running VMI through the KubeVirt Launcher Pod (a transient hotplug attachment pod hands the volume to virt-launcher). Inside the VM the guest kernel discovers a brand-new SCSI disk."
      },
      {
        "step": 9,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "csi-node-guest",
        "description": "The in-VM Kubelet sees the attached volume and calls the CSI Node Driver over its node socket. NodeStageVolume finds the hotplugged disk by serial ID, formats it on first use, and mounts it at the staging path; NodePublishVolume then bind-mounts the staged filesystem into the Pod's volume directory under /var/lib/kubelet/pods/<uid>/volumes/."
      },
      {
        "step": 10,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "frontend-application-pod",
        "description": "Kubelet has CRI-O start the container with the volume bind-mounted into its mount namespace. The application writes to a plain directory — beneath it: a bind mount, an ext4 filesystem on a hotplugged virtio-scsi disk, a management-cluster PVC, and finally a real block device on bare metal."
      }
    ]
  }
]
