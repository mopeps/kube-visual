[
  {
    "eventId": "route-ingress-traffic",
    "eventName": "External Ingress Traffic Flow",
    "description": "Tracing an inbound HTTPS request from an external web client through the bare metal cluster's MetalLB-advertised ingress VIP and proxy, across the OVS overlay fabric, into the KubeVirt VM, and — via the guest ingress VIP and the front-end ClusterIP Service — down to the front-end workload Pod.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "svc-ingress-lb-shared",
        "description": "Client sends an HTTPS request to the Route hostname, which resolves to the Shared Ingress LoadBalancer VIP. With no cloud LB present, a MetalLB speaker answers ARP for the VIP, so the packet lands on the bare metal cluster."
      },
      {
        "step": 2,
        "sourceComponentId": "svc-ingress-lb-shared",
        "targetComponentId": "shared-ingress-proxy",
        "description": "The LoadBalancer Service DNATs the VIP to a healthy Shared Ingress Proxy endpoint. HAProxy terminates TLS and identifies the guest cluster backend for this Route."
      },
      {
        "step": 3,
        "sourceComponentId": "shared-ingress-proxy",
        "targetComponentId": "ovs-host",
        "description": "HAProxy forwards the decrypted request into the bare metal worker node's OVS br-int bridge, which applies OVN flow rules to route the packet toward the KubeVirt Launcher."
      },
      {
        "step": 4,
        "sourceComponentId": "ovs-host",
        "targetComponentId": "kubevirt-launcher",
        "description": "OVS routes the packet through the tap0 virtual interface into the KubeVirt Launcher Pod, crossing the host-to-VM network boundary."
      },
      {
        "step": 5,
        "sourceComponentId": "kubevirt-launcher",
        "targetComponentId": "guest-worker-node-vm",
        "description": "The KubeVirt Launcher delivers the packet to the virtio-net NIC of the Guest Worker Node VM, shifting context from the host kernel to the guest RHCOS kernel."
      },
      {
        "step": 6,
        "sourceComponentId": "guest-worker-node-vm",
        "targetComponentId": "ovs-guest",
        "description": "The guest RHCOS kernel hands the packet to the in-VM Open vSwitch service, which evaluates guest OVN flow rules to route it toward the ingress VIP."
      },
      {
        "step": 7,
        "sourceComponentId": "ovs-guest",
        "targetComponentId": "svc-ingress-lb-guest",
        "description": "Guest OVS matches the Ingress LoadBalancer VIP (the router-default external IP advertised by MetalLB inside the guest cluster) and applies the OVN load-balancer flow for it."
      },
      {
        "step": 8,
        "sourceComponentId": "svc-ingress-lb-guest",
        "targetComponentId": "openshift-ingress-router-guest",
        "description": "The LoadBalancer Service DNATs the VIP to an OpenShift Ingress Router Pod endpoint inside the VM, which matches the request against its Route table."
      },
      {
        "step": 9,
        "sourceComponentId": "openshift-ingress-router-guest",
        "targetComponentId": "svc-frontend",
        "description": "The guest Ingress Router forwards the request to the Front-End Service — the ClusterIP that fronts the frontend Pods — addressing its stable virtual IP rather than a Pod directly."
      },
      {
        "step": 10,
        "sourceComponentId": "svc-frontend",
        "targetComponentId": "frontend-workload-pod",
        "description": "OVN load-balancer flows DNAT the ClusterIP to a ready Front-End Workload Pod endpoint. The application process accepts the connection and returns a response."
      }
    ]
  },
  {
    "eventId": "pod-spawning",
    "eventName": "Guest Pod Lifecycle (Spawn)",
    "description": "Tracing how a new workload Pod is scheduled and started in the guest cluster: from a kubectl apply hitting the Guest API Server through scheduling, kubelet notification, and CRI-O container creation inside the Guest VM.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "guest-api-server",
        "description": "Developer runs 'oc apply -f deployment.yaml'. The request reaches the Guest API Server, which validates it, persists the Deployment to etcd, and notifies controllers via watch events."
      },
      {
        "step": 2,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "guest-kube-scheduler",
        "description": "The Guest Scheduler watches for Pods with no node assignment. It evaluates guest worker node capacity and affinity rules, then selects the Guest Worker Node VM."
      },
      {
        "step": 3,
        "sourceComponentId": "guest-kube-scheduler",
        "targetComponentId": "guest-api-server",
        "description": "The Scheduler writes a Binding object back to the Guest API Server, officially assigning the Pod to the Guest Worker Node."
      },
      {
        "step": 4,
        "sourceComponentId": "guest-api-server",
        "targetComponentId": "kubelet-guest",
        "description": "The guest kubelet maintains its own outbound watch on the Guest API Server (via the exposed apiserver endpoint) and observes the newly bound Pod assigned to its node. The kubelet pulls its workload — the API Server never pushes to it, and this path does not use the Konnectivity tunnel (that tunnel only carries API-Server-initiated traffic such as exec/logs/port-forward)."
      },
      {
        "step": 5,
        "sourceComponentId": "kubelet-guest",
        "targetComponentId": "crio-guest",
        "description": "Kubelet validates the spec and issues RunPodSandbox and CreateContainer gRPC calls to guest CRI-O, which pulls the image, sets up the cgroup hierarchy, and invokes the CNI plugin to provision the Pod's network namespace."
      },
      {
        "step": 6,
        "sourceComponentId": "crio-guest",
        "targetComponentId": "frontend-workload-pod",
        "description": "Guest CRI-O starts the container via runc. The workload process becomes PID 1 inside its namespaces, registers with the guest network, and the Pod transitions to Running status."
      }
    ]
  },
  {
    "eventId": "east-west-service-traffic",
    "eventName": "East-West Service Traffic (Front-End → Back-End)",
    "description": "Tracing an in-cluster call from the Front-End Workload to the Back-End Workload through the Back-End ClusterIP Service: name resolution to a virtual IP, OVN load-balancer DNAT on the guest OVS fabric, and delivery to a backing Pod — no ingress, no traffic leaving the VM.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "frontend-workload-pod",
        "targetComponentId": "svc-backend",
        "description": "The Front-End resolves backend.e-commerce-prod.svc.cluster.local to the Back-End Service's ClusterIP and opens a connection to that stable virtual IP rather than to a specific backend Pod."
      },
      {
        "step": 2,
        "sourceComponentId": "svc-backend",
        "targetComponentId": "ovs-guest",
        "description": "The connection to the ClusterIP enters the in-VM OVS br-int bridge, where the OVN-Kubernetes load-balancer flow installed for this Service matches the virtual IP and selects a healthy backend endpoint."
      },
      {
        "step": 3,
        "sourceComponentId": "ovs-guest",
        "targetComponentId": "backend-workload-pod",
        "description": "OVS DNATs the destination from the ClusterIP to the chosen Back-End Workload Pod IP and delivers the packet over its veth pair. The backend process accepts the connection and returns its response."
      }
    ]
  },
  {
    "eventId": "hcp-provisioning",
    "eventName": "HCP Control Plane + NodePool Provisioning",
    "description": "Tracing how a new Hosted Control Plane and its first KubeVirt worker come up: the HyperShift Operator reconciles the HostedCluster, the per-HCP control-plane-operator brings up etcd and the Guest API Server, and the NodePool is turned into a VirtualMachineInstance via Cluster API and the KubeVirt provider.",
    "steps": [
      {
        "step": 1,
        "sourceComponentId": "external-client",
        "targetComponentId": "mgmt-kube-apiserver",
        "description": "Platform admin applies a HostedCluster and a NodePool manifest via 'oc apply'. These CRs live in the management (bare metal) cluster, so the Management Kube API Server persists them and notifies the HyperShift Operator."
      },
      {
        "step": 2,
        "sourceComponentId": "mgmt-kube-apiserver",
        "targetComponentId": "hypershift-operator",
        "description": "The HyperShift Operator (a cluster-wide singleton in the 'hypershift' namespace) reconciles the HostedCluster CR: it creates the dedicated HCP namespace and deploys the per-HCP control-plane-operator into it."
      },
      {
        "step": 3,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "control-plane-operator",
        "description": "The control-plane-operator becomes the per-hosted-cluster owner of the guest control plane, reconciling each control-plane component (etcd, Guest API Server, OAuth, Konnectivity, Ignition) inside the HCP namespace."
      },
      {
        "step": 4,
        "sourceComponentId": "control-plane-operator",
        "targetComponentId": "guest-etcd",
        "description": "The control-plane-operator deploys the etcd StatefulSet. Once etcd reaches quorum it brings up the Guest API Server, OAuth Server, Konnectivity Server, and Ignition Server as Deployments."
      },
      {
        "step": 5,
        "sourceComponentId": "hypershift-operator",
        "targetComponentId": "capk-provider",
        "description": "In parallel, the HyperShift Operator reconciles the NodePool into Cluster API objects (Cluster, MachineDeployment). The cluster-api-provider-kubevirt (CAPK) running in the HCP namespace requests a KubeVirt VirtualMachine for each desired replica."
      },
      {
        "step": 6,
        "sourceComponentId": "capk-provider",
        "targetComponentId": "kubevirt-launcher",
        "description": "CAPK creates a VirtualMachine object; KubeVirt's virt-handler on a bare metal worker node launches the virt-launcher Pod, which starts the qemu-kvm process and boots the RHCOS VirtualMachineInstance."
      },
      {
        "step": 7,
        "sourceComponentId": "ignition-server",
        "targetComponentId": "guest-worker-node-vm",
        "description": "As RHCOS boots, the VM fetches its Ignition config from the Ignition Server over TLS. The payload carries the kubelet bootstrap certificates and machine config needed to join the guest cluster."
      },
      {
        "step": 8,
        "sourceComponentId": "konnectivity-agent",
        "targetComponentId": "kubelet-guest",
        "description": "Inside the VM the Konnectivity Agent dials out to the Konnectivity Server, and the guest kubelet registers the node with the Guest API Server. The worker goes Ready and the hosted cluster can schedule workloads."
      }
    ]
  }
]
